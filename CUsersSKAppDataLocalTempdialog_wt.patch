diff --git a/components/AirportTransferBookingDialog.tsx b/components/AirportTransferBookingDialog.tsx
index 1d285af..d78bb22 100644
--- a/components/AirportTransferBookingDialog.tsx
+++ b/components/AirportTransferBookingDialog.tsx
@@ -1947,12 +1947,16 @@ function FareStep({ back, onSuccess, onRedirecting, booking }: { back: () => voi
       const res = await fetch('/api/bookings/manual', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(body) })
       const json = await res.json()
       if (!res.ok) { setSubmitError(true); setSubmitting(false); return }
+      const bookingNumber: string = json?.data?.booking_number ?? json?.booking_number ?? json?.data?.booking_id ?? json?.booking_id ?? ''
       const checkoutUrl: string = json?.data?.payment?.checkout_url ?? json?.payment?.checkout_url ?? ''
       if (checkoutUrl) {
+        if (bookingNumber) {
+          try { localStorage.setItem('whiteline.pendingBookingRef', bookingNumber) } catch {}
+        }
         onRedirecting?.()
         window.location.href = checkoutUrl
       } else {
-        onSuccess(json?.data?.booking_id ?? json?.booking_id ?? '')
+        onSuccess(bookingNumber)
       }
     } catch {
       setSubmitError(true)
